# Ce document est chiffré à l'aide de l'algorithme de substitution par mot clé
# Vous devrez coder une fonction de DECHIFFREMENT (et non de chiffrement) de substitution prenant en paramètres :
#   -Un mot/phrase à déchiffrer (en minuscule)
#   -Une clé étant une chaine de caractères (en minuscule)
# et retournant le mot déchiffré (en minuscule)
#
# La définition de la fonction vous est déjà donnée, à vous de l'implémenter
#
# Vous devrez également trouver la valeur de la variable cle. Vous la trouverez peut-être en fouillant le contenu de
# votre pc
#
# Aides :
#   - l'ajout d'une valeur en fin de tableau se fait avec la méthode append()
#   - pour savoir si une valeur existe dans un tableau, on utilise : if valeur in tableau
#   - il vous sera surement utile d'avoir une variable contenant l'alphabet minuscule dans l'ordre
#   - pour récupérer la position d'un élément dans un tableau, on utilise la méthode index()

# A compléter
cle = None


def substitution(word, key):
    # A compléter
    return

